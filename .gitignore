# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Regeneratable evaluation outputs (anchored to repo root; the published
# per-seed results under eval_metabox/results/ are tracked on purpose)
/results/
/RESULTS/

# TensorBoard events
*.tfevents.*

# Model weights: ignore by default, but keep the deployed checkpoint that ships with the release
*.pth
*.pt
!checkpoint/published_checkpoint.pth
